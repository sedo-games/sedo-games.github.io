GameView = Backbone.View.extend({

    //template: _.template($('#tpl-game-view').html()),

    render: function (eventName) {
      //  $(this.el).html(this.template(this.model.toJSON()));
      //  return this;
    }

});